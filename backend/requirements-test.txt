# Dépendances supplémentaires pour lancer la suite de tests backend.
# Installer en plus de requirements.txt :
#   pip install -r requirements.txt -r requirements-test.txt
#
# (httpx est nécessaire pour fastapi.testclient.TestClient)

pytest>=8.0
httpx>=0.27
pytest-cov>=5.0

# Test de charge (locustfile.py) — voir LOAD_TESTING.md
locust>=2.20